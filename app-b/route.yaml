apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: app-b
  name: app-b
spec:
  port:
    targetPort: http
  to:
    kind: "Service"
    name: service-b
    weight: null
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect