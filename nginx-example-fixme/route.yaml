---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: nginx-example-fixme
spec:
  to:
    kind: Service
    name: nginx-example-fixme
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
